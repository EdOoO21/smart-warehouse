groups:
  - name: smart-warehouse
    rules:
      - alert: WarehouseConsumerLagHigh
        expr: consumer_lag > 5
        for: 30s
        labels:
          severity: warning
        annotations:
          summary: Warehouse consumer lag is above threshold
